# React User Fetch App

## Features
- Fetches users from API using useEffect and fetch
- Displays loading and error messages
- Uses UserCard component for clean UI
- Refresh button to re-fetch data

## How to Run
1. Clone the repo
2. Run `npm install`
3. Run `npm start`

## API Used
https://jsonplaceholder.typicode.com/users
